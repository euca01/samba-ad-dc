#!/usr/bin/expect

spawn /usr/sbin/kstash
expect "Master key:"
send -- "$env(KERBEROS_PASSWORD)\r"
expect "Verifying password - Master key:"
send -- "$env(KERBEROS_PASSWORD)\r"
expect "\r"
send -- "exit\r"
expect eof
